# Step 1: API skeleton
fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.28.0,<0.30.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Step 2: Retrieval client
httpx>=0.27.0,<0.28.0
tenacity>=8.2.0,<9.0.0

# Step 3: Gemini integration — google-genai is the CURRENT SDK.
# google-generativeai (the older package) is being deprecated in favor
# of this unified SDK, which supports passing a Pydantic model directly
# as response_schema for enforced structured output.
google-genai>=1.0.0,<2.0.0
